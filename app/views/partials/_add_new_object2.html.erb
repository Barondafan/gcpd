<!-- Add a button for new instances of object -->
  <%= link_to(eval("new_#{model_name}_path(officer_id: @officer.id)"), class: "waves-effect waves-light btn") do %>
    <strong>Add a new <%= model_name.humanize.downcase %></strong>
  <% end %>